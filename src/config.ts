// The API endpoint will serve as the designated
// route for all requests within the useAPI hook. 😉

export const env = {
    API: 'http://192.168.100.123:3005/api',
    ALLOWED_HOSTS: ['freeddos.pw']
}